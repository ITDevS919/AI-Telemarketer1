import React from 'react';

export default function Dashboard() {
  return (
    <section>
      <h1 className="section-title">Dashboard</h1>
      <p className="section-subtitle">
        Overview of your AI telemarketer activity will appear here.
      </p>
      <p>Use the navigation on the left to start calls or manage cloned voices.</p>
    </section>
  );
}

